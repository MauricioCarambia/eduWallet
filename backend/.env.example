# Servidor
PORT=3001
NODE_ENV=development

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eduwallet
DB_USER=postgres
DB_PASSWORD=

# Autenticación
JWT_SECRET=cambiar_por_secreto_seguro_en_produccion
SUPERADMIN_SECRET=cambiar_por_secreto_seguro_en_produccion

# Mercado Pago
MP_ACCESS_TOKEN=
MP_PUBLIC_KEY=

# Mercado Pago — OAuth (conectar colegio y empleados, split payments)
# Se consiguen creando una Aplicación en mercadopago.com.ar/developers
MP_CLIENT_ID=
MP_CLIENT_SECRET=

# URLs (sin barra al final)
# En desarrollo: BACKEND_URL puede ser http://localhost:3001 con ngrok para recibir webhooks
BACKEND_URL=https://tu-backend.onrender.com
PADRES_URL=https://tu-frontend-padres.vercel.app
ADMIN_URL=https://tu-frontend-admin.vercel.app
POS_URL=https://tu-frontend-pos.vercel.app

# Email (Resend)
RESEND_API_KEY=
FROM_EMAIL=notificaciones@tudominio.com

# Notificaciones push (PWA padres)
# Generar con: node -e "console.log(require('web-push').generateVAPIDKeys())"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@tudominio.com
